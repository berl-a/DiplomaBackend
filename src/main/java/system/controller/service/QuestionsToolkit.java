package system.controller.service;

import java.util.LinkedList;

public class QuestionsToolkit {

//    public static boolean listContainsAnswer(LinkedList<OneAnswer> list, OneAnswer answer) {
//        return list
//                .stream()
//                .anyMatch(
//                        listElement ->
//                                (listElement.getText() != null && listElement.getText().equals(answer.getText())) &&
//                                        (listElement.getImage() != null && listElement.getImage().equals(answer.getImage()))
//                );
//    }
}
